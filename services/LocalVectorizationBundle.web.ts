// Import the local web adapter so Metro emits the generated WASM assets,
// without changing the active vectorization flow or instantiating WASM at startup.
import './LocalVectorizationService';

export {};
